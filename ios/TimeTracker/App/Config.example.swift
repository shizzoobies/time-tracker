// Config.example.swift — committed to git, copy to Config.swift and fill in real values
enum Config {
    static let supabaseURL = "https://YOUR_PROJECT.supabase.co"
    static let supabaseKey = "YOUR_SUPABASE_ANON_KEY"
}
